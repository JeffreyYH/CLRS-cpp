#include <iostream>
using namespace std;

template <class T>
class HashTable
{
public:
    direct_address_search(k);
    direct_address_insert(x);
    direct_address_delete(x);
    hash_insert(k)
    hash_search(k)
};